CREATE TABLE IF NOT EXISTS item_asset (
  id INTEGER NOT NULL,
  image_path TEXT,
  PRIMARY KEY(id)
);

deleteAll:
DELETE FROM item_asset;

insert:
INSERT OR REPLACE INTO item_asset VALUES (:id, :imagePath);
